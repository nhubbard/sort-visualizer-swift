import XCTest

// Empty placeholder — BuiltInAlgorithms is empty again per §2.6 now that quicksort.js proves the
// scripted path covers what the Phase 2 native proof-of-concept needed to prove. Keeps the test
// target's bundle loadable (an XCTest bundle with zero source files fails to load at all).
final class BuiltInAlgorithmsPlaceholderTests: XCTestCase {}
