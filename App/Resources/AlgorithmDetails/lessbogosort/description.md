*From Wikipedia, the free encyclopedia*

Less Bogosort is a variant of [Bogosort](https://en.wikipedia.org/wiki/Bogosort) that narrows its randomness down to a shrinking suffix of the array instead of repeatedly gambling on the whole thing at once. Starting from the first position, it repeatedly applies a Fisher–Yates shuffle to the unsorted range `[i, n)` — the elements from the current position to the end of the array — checking after each shuffle whether the element that landed at position `i` happens to be the smallest value anywhere in that range. The moment it does, position `i` is locked in as correct and the algorithm moves on to `i + 1`, so the region still being shuffled shrinks by one element every time a position is settled.

This is a meaningful departure from classic Bogosort, which reshuffles the *entire* array on every failed attempt and only stops once the whole thing happens to come up fully sorted in one shot. Because Less Bogosort never re-disturbs positions it has already confirmed, each successive shuffle operates on a smaller range and therefore needs, on average, far fewer attempts to land the front element correctly than ordinary Bogosort needs to land an entire array correctly. The trade-off is that it still relies on pure chance for every single position: a small unsorted suffix can still take an arbitrarily long, unbounded number of shuffles before the right element happens to land in front, which is why the algorithm keeps Bogosort's family trait of being unsuitable for anything but demonstration.

Less Bogosort has a mirror-image sibling, Cocktail Bogosort, which shrinks from both ends of the array at once — alternating between confirming a minimum at the front of the remaining range and a maximum at the back, so the shuffled middle region shrinks by one from each side per pass rather than by one from the front alone. Both belong to the same lineage of algorithms that trade Bogosort's single giant gamble for a sequence of smaller ones.

Despite the resemblance to distribution-style sorts in name and package placement, Less Bogosort is not a distribution sort in any meaningful algorithmic sense — it performs no bucketing or classification of values. In ArrayV, it is classified among the "Impractical Sorts," the family reserved for algorithms whose only real purpose is to illustrate, by absurd example, what *not* to do when sorting.
