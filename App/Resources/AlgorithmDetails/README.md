# Algorithm Content Pipeline

Generates the syntax-highlighted code samples the app displays (via
`Modules/DesignSystemKit/Sources/CodeAttributes.swift`'s Pygments-token-based markdown).
Originally `Tools/AlgorithmContentPipeline/`, preserved from `Legacy/Shared/Resources/` when the
rest of `Legacy/` was removed (Phase 11) — this was the only part of `Legacy/` still in active use,
and was later folded into `App/Resources/AlgorithmDetails/` so the pipeline and its output live in
one place.

## Layout

Each algorithm has exactly one folder, `<algorithm-id>/`, holding both the authoring source and
the finished content side by side:

- raw source, `<algorithm-id>.<ext>` for `ext` in `c cpp cs go java js kt py rb swift`
- `description.md`
- one `<lang>.md` per language, generated by `manage.py highlight`, named to match
  `CodeLanguage.all`'s ids (`py.md`, `js.md`, `go.md`, `java.md`, `c.md`, `cpp.md`, `cs.md`,
  `rb.md`, `kt.md`, `swift.md`) — packed by content-kind index, which matches this same language
  order.

None of this folder's raw content reaches the app bundle directly. `manage.py pack` compresses
every `description.md` and generated `<lang>.md` into one `AlgorithmDetails.algz` file, which
`Project.swift` ships as a single ordinary resource; `AlgorithmDetailStore`
(`Modules/SortFeature/Sources/`) decompresses and parses it at runtime — see
`../../../COMPRESSION_DESIGN.md` for the full container format and runtime design. There's no
per-algorithm Tuist build phase anymore: adding a new algorithm just means adding its folder here
and re-running `highlight`/`pack`.

Other root-level content, none of it shipped:

- `template/` — master template used by `manage.py scaffold` to start a new algorithm folder.
- `manage.py` — a Click-based CLI (`uv run manage.py <command> --help`, or `-h`):
  - `scaffold <name>` — creates `<name>/` from `template/`.
  - `highlight [name ...]` — runs Pygments over each algorithm's raw source, writing
    `<algorithm-id>/<lang>.md` directly in the `^[text](code: 'Token.X')` syntax `CodeAttributes`
    parses. Defaults to every algorithm; pass one or more IDs to limit it.
  - `test [name ...] [--languages/-l ...]` — compiles/runs each algorithm's raw source per
    language and checks output against an expected sorted result, to verify correctness before
    highlighting. Needs the actual toolchains installed (clang, javac/java, kotlinc, python3,
    ruby, swiftc, node, go, dotnet — whichever languages you're checking). Defaults to every
    algorithm and every language; `-l py -l js` etc. restricts it.
  - `pack [name ...] [--output PATH] [--level N] [--no-verify]` — compresses every algorithm's
    `description.md` and highlighted `<lang>.md` files into the single `AlgorithmDetails.algz`
    archive described in `../../../COMPRESSION_DESIGN.md` (one whole-corpus zstd
    frame, no dictionary, versioned `ALGZ`/`ADTL` container, outer SHA-256 + zstd content
    checksum). Self-verifies by default after writing — decompresses the archive back and
    compares every extracted byte range against the source `.md` files on disk; `--no-verify`
    skips that pass.
  - `decode [name ...] [--input PATH] [--preview-length N]` — a low-level visualizer for a
    packed archive's byte layout: the outer `ALGZ` envelope and inner `ADTL` header fields
    (flags, offsets, lengths, SHA-256), a bar chart of decompressed bytes by content kind, and
    per-algorithm directory records down to each content entry's raw header fields
    (kind/flags/offset/length/reserved) with a size bar and a one-line content preview — never
    the full Markdown. A sanity check that `pack` produced the exact byte layout the container
    spec describes, not just that the content round-trips. Defaults to every algorithm.
- `.gitignore` — one shared copy of the build-artifact ignore rules `manage.py test` needs
  (compiled binaries, `.class` files, etc.), covering every algorithm folder underneath it.
- `pyproject.toml`/`uv.lock` — pin `manage.py`'s dependencies (`click`, `pygments`,
  `coloredlogs`, `zstandard`, plus `ruff` as a dev-only lint/format dependency); managed with
  `uv add`/`uv remove`, never hand-edited.

## Usage

```sh
cd App/Resources/AlgorithmDetails
uv run manage.py highlight
uv run manage.py test
uv run manage.py pack
uv run manage.py decode quicksort
```

`uv run` provisions `.venv` from `pyproject.toml`/`uv.lock` automatically — no separate
`pip install` step. `highlight` writes straight to `<algorithm-id>/<lang>.md` — there's no
separate shipped folder to copy into afterward. `pack` writes `AlgorithmDetails.algz` at this
directory's root; it is a derived build artifact regenerated from the tracked `description.md`
and highlighted `.md` files, not a hand-edited source file itself.

## Known naming/coverage gaps

- `lsdradixsort/` ships without any raw source behind it.
